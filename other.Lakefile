target "busybox_shell" {
  inputs = [busybox_store]
  script = <<EOH
    #!${busybox_tar}/bin/busybox sh
    export PATH=$busybox_store/bin/
    sh $@
  EOH
}

store "empty_store" {
  inputs = []
  script = <<EOH
    sh ./script.sh
  EOH
}
