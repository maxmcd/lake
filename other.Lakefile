import = [
  "lake/lib/busybox",
]


target "busybox_shell" {
  script = <<EOH
    #!${busybox.busybox_tar}/bin/busybox sh
    export PATH=$busybox_store/bin/
    sh $@
  EOH
}

store "empty_store" {
  inputs = []
  script = <<EOH
    sh ./script.sh
  EOH
}
